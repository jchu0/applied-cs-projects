# Build / dependency artifacts
target/
**/target/
# Cargo.lock: these crates are libraries/teaching projects — not committed
# (repo-wide convention). Deployable binaries would normally commit it.
Cargo.lock
__pycache__/
*.py[cod]
.venv/
venv/
.env
node_modules/
dist/
build/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.DS_Store

# Development tracker / process files — kept locally, not committed
PROGRESS.md
PROJECTS_STATUS.md
IMPLEMENTATION_PLAN.md
audit/

# Internal doc-authoring framework — referenced locally, not pushed
DOC_STYLE_GUIDE.md
templates/

# Benchmark/run output artifacts
results/
